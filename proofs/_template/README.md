# BeeJSP Lean template

This directory is a template/smoke project only. It is not a Justin Sun Prize
submission. It provides the smallest pinned Lean project for verifying the local
toolchain and build path.

Run `lake build` from this directory.
