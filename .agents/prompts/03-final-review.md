# BeeJSP final review

Use only Bee Dev MCP.

Read:

- `AGENTS.md`
- `.agents/skills/beejsp-review-and-close/SKILL.md`

## Primary target

- Project: `beejsp`
- Instruction worktree: `<INSTRUCTION_WORKTREE>`
- Target worktree: `<TARGET_WORKTREE>`
- Expected branch: `<FEATURE_BRANCH>`
- Base branch: `main`
- Mode: `<MODE>`
- Roadmap context: `<ROADMAP_CONTEXT>`

## Approved Issue

```text
<FULL_APPROVED_ISSUE>
```

## Current PR

```text
<CURRENT_PR_OR_NONE>
```

## Implementation and verification evidence

```text
<CURRENT_IMPLEMENTATION_EVIDENCE>
```

## Additional targets

```text
<ADDITIONAL_TARGETS_OR_NONE>
```

Rules:

- Perform one complete read-only review pass.
- For additional targets, follow the supplied `Role`, `Mode`, and `Skill`.
- Actual manifest, diff, files, package contents, proof contents, contracts, and verification evidence take precedence over Issue, PR, and implementation reports.
- Do not modify files or run commands.
- Do not add optional polish, new requirements, or unrelated cleanup.
- Do not require `uv lock --check`.
- Return the result strictly according to the review skill.
